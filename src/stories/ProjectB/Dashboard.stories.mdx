import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { Homepage } from '../HomepageB';

<Meta
  title="Project B (MDX)/Dashboard"
  scopes={['developers-b']}
  component={Homepage}
/>

export const Template = (args) => <Homepage {...args} />;

# Dashboard

<Canvas>
  <Story name="Primary">
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Designer Only Story" scopes={['designers']}>
    {Template.bind({})}
  </Story>
</Canvas>